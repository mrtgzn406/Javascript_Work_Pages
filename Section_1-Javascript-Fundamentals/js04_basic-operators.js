
      const now = 2022;

      const ageJonas = now - 1991;
      const ageMichael = now - 2003;

      console.log(ageJonas * 2, ageJonas / 10, 2 ** 4);
      // ! 2 ** 4 meaning  4 pieces of 2 wil be multiplied each other one by one. // 2 * 2 * 2 * 2

      const firstName = "Ömer";
      const lastName = "İşbilir";

      console.log(firstName + " " + lastName);

      // ! Assignment operators
      let x = 10 + 5; // 15
      x += 10; // x = x + 10  = 25
      x *= 4; // x = x * 4 = 100
      x++; // x = x +1 = 101

      x--; //x = x -1 = 100

      console.log(x);

      // ! comparison operators

      console.log(ageJonas < ageMichael);
      console.log(ageJonas >= ageMichael);

