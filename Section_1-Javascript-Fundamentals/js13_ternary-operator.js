
      const yas = 25;

      const isAdult = yas >= 18 ? "you are an adult" : " you are not an adult";

      console.log(isAdult);
      // -------------------------------------------------------------------------

      const mainColor = "yellow";

      console.log(
        mainColor === "red"
          ? "red is a main color"
          : mainColor === "blue"
          ? "blue is a main color"
          : mainColor === "green"
          ? "green is a main color "
          : "this is not a main color"
      );

      myAge = 14;

      console.log(
        `I ${myAge >= 18 ? "can" : "can't"} vote in upcoming election`
      );

